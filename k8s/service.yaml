apiVersion: v1
kind: Service
metadata:
  name: kubernetesdemoapp-service
  namespace: default
  labels:
    app: kubernetesdemoapp
spec:
  type: NodePort
  selector:
    app: kubernetesdemoapp
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
